{
  "name": "records-and-tuples",
  "version": "1.1.0",
  "description": "Utilities to create immutable objects and arrays",
  "main": "./dist/index.min.js",
  "module": "./dist/index.min.js",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "rimraf dist && tsc && esbuild --bundle --platform=neutral --main-fields=module,main --minify --outfile=dist/index.min.js dist/index.js"
  },
  "keywords": [
    "immutable",
    "array",
    "object",
    "record",
    "tuple"
  ],
  "author": "Simon de Bruijn",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.15.15",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  }
}
